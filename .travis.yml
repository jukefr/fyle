language: node_js
node_js:
  - "10"
cache:
  directories:
  - /home/travis/.nvm/versions/node/v10.9.0/bin/
  - /home/travis/.nvm/versions/node/v10.9.0/lib/node_modules/
sudo: required
services:
  - docker
before_install:
  - npm i -g vuepress
  - bash build.sh travis
script:
  - bash build.sh
  - bash test.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: docs/.vuepress/dist
  target-branch: gh-pages
  on:
    branch: master